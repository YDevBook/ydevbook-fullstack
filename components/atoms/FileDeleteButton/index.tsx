'use client';

import { RiDeleteBinLine } from '@remixicon/react';
import { Button } from '@tremor/react';

const FileDeleteButton = () => {
  return <Button variant="light" icon={RiDeleteBinLine}></Button>;
};

export default FileDeleteButton;
